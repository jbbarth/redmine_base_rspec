gem 'rspec-rails', '~> 5.0'
gem 'rails-controller-testing'
#for test coverage
# gem 'simplecov', '~> 0.9.1', :require => false, :group => :test

# To make the core tests pass with Redmine 4, we need to add some dependencies (remove when we stop supporting Redmine 4)
gem "builder", :group => :test
